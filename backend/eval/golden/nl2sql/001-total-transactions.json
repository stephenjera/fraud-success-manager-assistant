# Golden NL->SQL fixtures (question + reference SQL the agent should produce).
# The concrete pairs currently run live in eval/promptfoo/nl2sql.yaml (tests.vars.reference_sql).
# Complements the Python deterministic runner (harness.py) rather than replacing any tool.
